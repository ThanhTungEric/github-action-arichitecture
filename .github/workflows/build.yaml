name: nodejs-hello-world

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  merge-master:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@master

      - name: Merge master -> chithanh
        uses: chithanh/merge-branch@master
        with:
          type: now
          from_branch: master
          target_branch: chithanh
          github_token: ${{ secrets.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 }}